# Required

# The URL of the image to fetch and display (viewport appended automatically)
IMAGE_URL="http://homeassistant.local:10000/inky-dashboard?kiosk"

# Optional

# Bearer token to send with the image request, if required
IMAGE_TOKEN="your_long_lived_token_here"
# E-ink saturation - tweak to help improve the image quality
DISPLAY_SATURATION=0.1
# How fast the LED blinks when the display is refreshing (seconds)
LED_BLINK_INTERVAL=0.2
# How long to wait before refreshing the display again (seconds)
DISPLAY_WAIT_TIME=1800

# The following are used for deployment to a Raspberry Pi.
# These should match what you have setup.

# The hostname or IP address of the Raspberry Pi
RPI_HOST="inky"
# The username to use when connecting
RPI_USER="inky"
# Directory where the code will be deployed
REMOTE_DIR="/home/inky/src/inky-dashboard"
